[gcode_macro block_timeout]
variable_blocked: 0
gcode:
  {% set state = params.BLOCK|default(1)|int %}
  SET_GCODE_VARIABLE MACRO=block_timeout VARIABLE=blocked VALUE={state}

[gcode_macro unblock_timeout]
gcode:
  SET_GCODE_VARIABLE MACRO=block_timeout VARIABLE=blocked VALUE=0
